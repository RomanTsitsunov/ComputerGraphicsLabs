name: GitHub Actions Demo

run-name: ${{ github.actor }} is testing out GitHub Actions 🚀(Поехали!)

on:
  push:
    branches:
    - main
    - dev
jobs:
  Explore-GitHub-Actions:
    runs-on: self-hosted
    steps:
      - name: Check out repository code